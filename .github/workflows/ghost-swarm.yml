name: Ghost Swarm Legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Legacy workflow retired
    runs-on: ubuntu-slim
    timeout-minutes: 2
    steps:
      - name: Confirm retirement
        run: echo "Legacy swarm retired; use ghost-throughput.yml and ghost-intelligence.yml."
